<%= simple_form_for [@category, @story, @story.comments.build] do |f| %>
<%= f.error_notification %>
<%= f.input :body, label: false, placeholder: "Your comment" %>
<%= f.button :submit, class: "btn-primary" %>
<% end %>
